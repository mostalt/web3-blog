
Install the dependencies

```sh
npm install
```

Run the local node

```sh
npx hardhat node
```

Deploy to localhost

```sh
npx hardhat run scripts/deploy.js --network localhost
```

Start the app

```sh
npm start
```
